Steps to add and configure spring security:

1 - add dependency

    in maven:

    <dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
    </dependency>

2 - create a security configuration class

